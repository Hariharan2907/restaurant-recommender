# Shared local dev settings — also see backend/.env.example and mobile/.env.example
# This file documents the shared contract used by docker-compose.yml.
# Compose itself hardcodes these dev values; this file is purely documentation.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=restaurant_recommender
POSTGRES_PORT=5432
REDIS_PORT=6379
BACKEND_PORT=8000
